interface:
  display_name: "Bilibili Topic KB Builder"
  short_description: "Build Bilibili topic or uploader-space crawlers with exports, retries, and a local web viewer."
  default_prompt: "Use $bilibili-topic-kb-builder to build, adapt, or review a Bilibili topic or uploader-space knowledge-base pipeline with JSONL exports, retry flows, and a local web viewer."
policy:
  allow_implicit_invocation: true
